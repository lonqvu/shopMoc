# Backend

Đây là vị trí dành cho API và business logic phía server của Shop.

Backend hiện chưa được khởi tạo. Khi xây dựng backend, các file như `package.json`, `.env.example`, source, migration và test của server sẽ nằm trong thư mục này, tách biệt với ứng dụng React tại `../frontend`.
